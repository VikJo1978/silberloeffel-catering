# Install as /etc/catering/staging-site.env on the VPS with owner root:root
# and mode 600. The real bearer must never be committed or passed in argv.

STAGING_CORE_INTAKE_URL=http://127.0.0.1:18083/intake/website-form
STAGING_CORE_INTAKE_TOKEN=REPLACE_WITH_STRONG_RANDOM_TOKEN
